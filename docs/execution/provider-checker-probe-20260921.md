# Checker compatibility probe correction — 21 September 2026

The saved DeepSeek answer probe passed, while the first checker suite passed basic/structured stages and failed its project stage with HTTP 200 and a complete, nontruncated response. That original response was deliberately erased by the compatibility storage policy, so its exact validation cause cannot be reconstructed. The failed durable receipt remains unchanged in `evidence/week08-memory-v2/20260921/providers/deepseek-staged-live-01/checker.json`.

One separately authorized authored checker probe used the exact same managed configuration and a 4,096-token output reservation. Its JSON and `ReliableCheck` schema passed, but the calculation expression included physical units and an equality sign. The real arithmetic validator correctly rejected that human-readable expression. The probe had omitted the production checker's machine-readable expression grammar. This diagnostic used one call and reported 1,318 input/588 output tokens; raw fixture-only output is retained solely under `providers/private/checker-project-diagnosis-01`. No learner question, memory or credential is in the public diagnosis.

The bounded correction adds generic arithmetic-expression instructions to `generation/probes.py`: named inputs and the supported operators, with units and result in separate fields. It neither provides the solved response JSON nor relaxes arithmetic, quote, source-basis or schema checks. Failed probes now retain finite program-authored validation codes and schema-owned field locations. Unknown model keys become `unknown_field`; raw values, rejected content and exception messages remain excluded. Existing activation checks compare the current prompt/schema/configuration/capability hashes and effective reservations, so an old passing receipt cannot activate a changed probe.

The updated durable checker project test passed in one call: [actual receipt](../../evidence/week08-memory-v2/20260921/providers/deepseek-checker-project-live-02.json). The earlier six-stage attempt and the diagnostic call remain separate failures. **35 focused tests passed**, covering schema/prompt activation invalidation, strict arithmetic rejection, safe diagnostics and local HTTP model-settings lifecycle. Ruff and mypy passed. The final probe source SHA-256 is `d68c273298c9294806d7f01cb007a5dfa115f446d28e224b913aabb244f416a4`. [Safe diagnosis and source/test hashes](../../evidence/week08-memory-v2/20260921/providers/checker-project-diagnosis-summary.json) describe the earlier diagnostic snapshot; its `updated_live_pass=null` predates the separately linked successful durable test.

Compatibility success demonstrates this transport and authored contract, not general answer quality. The separate ATP product HTTP request `dd76c0cc-eeb7-401c-aa42-fe01900c950e` still failed after three calls. Both semantic checks labelled all seven claims supported; the first required two citation identities for one claim, and the fresh check selected only one after marker repair. The strict exact-set gate rejected the extra unselected marker and ended after its two permitted draft revisions. [The metadata-only diagnosis](../../evidence/week08-memory-v2/20260921/answering/product-http-atp-failure-diagnosis.json) records this remaining citation-binding instability. It is neither a negative factual judgment nor independent scientific validation, and the failed request was not altered or retried.
