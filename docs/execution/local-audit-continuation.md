# Additional local requirement audit — 8 September 2026

This continuation found and closed concrete local gaps after the previous source-package checkpoint. It does not replace the original 108 tasks, 60 checks or 12 responsive subchecks, and it does not infer overall completion from test counts.

| Requirement | Current observation | Evidence and remaining boundary |
| --- | --- | --- |
| Source section 20.6; INT-10, CHAT-12, QA-14 | A deterministic exporter now generates eight owner views, seven course-week views and a shared 60-check reference. Both groupings partition all 108 task IDs exactly once. | [Delivery entry](../delivery/README.md); [initial determinism/link check](../../evidence/delivery/2026-09-08T09-52-01Z.json); [duplicate/missing-input rejection](../../evidence/delivery/2026-09-08T09-56-03Z-input-validation.json). The generated manifest binds current ledger bytes. Owner/week metadata never limits implementation. |
| PER-03 | Twelve actual main-service turns verify all three saved levels, three source-establishing questions and nine short/long/simplification targets. Snapshots, policy, citations and one mock call per turn are checked. | [Report](profile-lengths-20260908.md). Six target answers retained the same short excerpt; all three long requests refused. Long-output and meaningful adaptation remain unverified. |
| PER-07 | Automatic validation checks structural, profile-version and evidence integrity; prompts contain preservation instructions. | The parser explicitly leaves semantic preservation null. Real facts, negation, formulas and examples still require independent review. |
| AC-34 | Five tests switch the same shared generation consumer mock → actual loopback HTTP → mock across chat, OpenQA and MCQ. Valid distinct authored responses, usage, request IDs and bounded failures are verified. | [Drill](adapter-replacement-drill.md). This is a controlled software fixture; GEN-02 still requires the user-deferred real answering provider. |
| AC-44; UI-11 | Real browser Enter/Shift+Enter, a saved suggestion click, polled stages and actual rejection/draft/history checks pass. A discovered clipped error is now automatically revealed at the latest reading position, while deliberate upward reading is preserved. | [Browser report](keyboard-suggestion-verification.md); [actual request/release binding](../../evidence/frontend/keyboard-request-binding-20260908T095633Z/verification.json). Full-alert checks pass at 1440/390. Physical/native/human subchecks UI-05/UI-09 remain unverified. |
| AC-19 | The original clause requires honest treatment of missing ratings and mock outputs. Missing values, immutable manifests, all outcomes and mode boundaries have software evidence. | Its software component is `MOCK_TEST_PASSED`; unavailable real runs/ratings remain explicit in QA-04, QA-07, QA-10 and PER-09. Missing ratings do not prevent checking whether the software reports them honestly. |

The unchanged-source aggregate gate completed at **2026-09-08T09:57:46Z** with **236 Python tests and 25 frontend tests**, plus formatting, lint, boundary typing, contracts, schema generation and production build. [Gate](../../evidence/final/software_gate.json). Its predecessor is preserved under `evidence/final/attempt-stable-0930-before-local-audit/`, including the exact original hashes. No test counts are summed across attempts.

Only one application file changed since the independently installed candidate 2: the error/terminal-state scroll dependency list in `frontend/src/Chat.tsx`. Its two new component regressions and the final real-browser run cover the changed behavior. Other changes are verification/export utilities, one focused adapter test, documents and evidence. Candidate 2 remains the actual earlier fresh-install proof; it is not relabelled as a new installation of this UI revision. The current production build and current source snapshot are separately verified.

The real four-book v5 corpus is unchanged at 10,594 actual 384-dimensional pinned E5 vectors. The new read-only browser companion confirms all six requests from all three retained browser runs used that actual release and configured embeddings. The complete 09:33 backup remains a point-in-time snapshot; the later verification accounts and conversations are retained in the main database and explicitly postdate it.

Real answering configuration, formal real-model results, independent relevance/scientific/teaching review and physical/native/human interface checks remain open. The missed original pre-R1 annotation chronology remains a recorded deviation; it cannot be repaired by backdating the late pool. This continuation does not create grades, scientific findings or an acceptance decision for those clauses.
