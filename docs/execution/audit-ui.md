# Frontend implementation and verification audit

Scope: original v5 FE-01–FE-12 and CHAT-06, together with the responsive refinement's UI-01–UI-12 subchecks. This audit supplements the unchanged 108-task and 60-scenario ledgers. It does not replace their acceptance criteria or mark human and live-model research as complete.

Current statuses use the five canonical project labels: **NOT_IMPLEMENTED** means the required artifact is absent; **IMPLEMENTED_UNVERIFIED** means implementation exists but a material acceptance clause remains unverified; **MOCK_TEST_PASSED** means the cited scope passed with simulated or authored inputs; **REAL_FLOW_VERIFIED** means the stated real component ran with its required dependencies and data; **WAITING_EXTERNAL** means a named unavailable external input prevents that exact check. Real browser/API evidence does not certify mock answer quality. The implementation and evidence boundary is stated for every row; no percentage completion is inferred.

| Scope | Current status | Verified scope and remaining limits |
|---|---|---|
| FE-01–FE-04, FE-06–FE-09, FE-11, CHAT-06 | REAL_FLOW_VERIFIED | Cited component/browser controls ran, including the final official-source learner journey; answer generation remains mock |
| FE-05 | MOCK_TEST_PASSED | Conversational rendering also has real browser evidence; the historical MCQ acceptance uses answered/refused read-only component fixtures, with no historical MCQ database mutation |
| FE-10 | REAL_FLOW_VERIFIED | Cited upload/process/inspect/deactivate/restore/build/activation controls and completed valid A→B→A browser rollback; historical integrity refusal preserved |
| FE-12 | IMPLEMENTED_UNVERIFIED | Cited browser, zoom, component and deployment checks passed; full physical-device/native/human acceptance remains unverified |
| UI-01–UI-03, UI-06–UI-08, UI-10–UI-12 | REAL_FLOW_VERIFIED | Cited actual browser component flows passed; current source/responsive evidence, actual default-size pagination, and separately scoped zoom and arrival-while-reading evidence remain distinct |
| UI-04 | MOCK_TEST_PASSED | The authored long-content stress fixture verifies wrapping and local scrolling; it does not establish generated-response or scientific quality |
| UI-05, UI-09 | WAITING_EXTERNAL | Desktop low-height/keyboard/focus and emulated touch/composition pass; physical keyboard/orientation, native OS IME and assistive-technology sessions remain unavailable |

`docs/execution/ui_acceptance.json` records all twelve subchecks with these canonical statuses and exact evidence paths. Its initial unexecuted checkpoint is retained explicitly as historical metadata. Whole-clause acceptance remains distinct from a passed component observation; the main 108/60 ledgers have separate owners.

## Evidence boundary

The browser journeys use the actual HTTP API, PostgreSQL persistence and worker. The generation provider is explicitly mock. Earlier uploaded browser materials and the long-content stress answer are authored software fixtures. The final official-source journey uses four genuine OpenStax PDFs and real local E5/pgvector retrieval. The stress fixture reuses a real stored source snapshot and hash but is not generated-response quality evidence. Request, answer, job, feedback and release IDs are recorded with each journey.

Screenshots are reviewed with document/body/element bounds, actual interaction and persisted records. The shell has no body/html horizontal-overflow suppression. Local code/table/formula scrolling is intentional; sidebar title ellipsis has a full title and focus expansion. Initial failures and their traces remain under timestamped report directories.

## Original task coverage

| Task | Implementation | Verification and limits |
|---|---|---|
| FE-01 | `frontend/src/App.tsx`, `api.ts`, `types.ts`, `generated/api.ts`; single typed React application, explicit API error envelopes, no hidden mock fallback | TypeScript production build; generated OpenAPI drift check; component and real API browser suites. Administrative dictionary payloads remain generic where exported by the backend. |
| FE-02 | Sign-in, expiry event, logout, role navigation and private-state clearing in `App.tsx`/`api.ts`; name/password controls in `Settings.tsx` | `account-security.spec.ts`: actual password change, old token 401, role route denial plus actual 403, private drafts removed, new credential re-login and saved name. `account.spec.ts`: actual logout and profile navigation. |
| FE-03 | Natural-language composer, draft persistence, Enter/Shift+Enter/composition guard, profile switch, stable idempotency in `Chat.tsx` | Component regressions inspect exact request bodies and stable keys after failure; real browser resize preserves draft and selection without POSTs. Native OS IME and physical keyboard sessions remain unverified. |
| FE-04 | Real durable job polling/stages/cancel/retry/terminal errors and re-login recovery in `Chat.tsx` | `lifecycle.spec.ts`: actual cancelled job, retry same logical request/new job with one user turn, failed replacement retention. Component test retains drafts after failure. No fake token streaming. |
| FE-05 | Full safe Markdown, code/tables/math, schema-directed historical MCQ rendering, copy and mode/profile labels in `Chat.tsx`/`Markdown.tsx` | Real no-SciQ journey verifies complete clipboard text and stored answer mode. Stress fixture verifies complete long prose and local scrolling. Component tests reject unsafe HTML/protocols and keep unknown citation syntax literal. Answered/refused `benchmark_mcq` / `mcq_response_v1` histories render through the full Chat component, retain the original evidence endpoint and ordinary composer, and perform no send mutation. Evidence: `evidence/ui/legacy-mcq-final-component-tests.json` (23 passed). This is component evidence, with no MCQ fixture inserted in the real database. |
| FE-06 | Original evidence endpoint, citation mapping, full source title/section/locators, hash/provenance details and unavailable-source state | Real browser exact passage/title match; retained snapshot hashes in `no-sciq-journey.json`; component 410 test proves cached passage is not substituted. Citation trigger identity/focus and withheld-metadata control regressions pass. |
| FE-07 | Three levels, style/interests, revision save/reset, applied snapshot details and per-turn profile switch | `account.spec.ts`, `account-security.spec.ts`, `chat.spec.ts`: saved/reset level and style, actual concurrent revision conflict, edit retained/reload saved version, profile-off request still carries conversation history. |
| FE-08 | Default `/chat`, sessions, new chat, rename/archive/restore, ordered paginated transcript and latest revision/job recovery | `session-controls.spec.ts`: real rename/archive/refresh/restore and per-session drafts. `chat.spec.ts`: re-login restores six messages and active regenerated answer. `lifecycle.spec.ts`: original revision and feedback survive failed replacement. |
| FE-09 | Stored helpfulness/comment create/update, saving/error/saved states; connected administrator review | Real chat saves and reopens existing feedback. `admin.spec.ts` saves and reloads review state/note/issue on the same answer. Component failure behavior keeps text. |
| FE-10 | `admin.tsx`: upload, versions/quality, processing/exclusions, deactivate/restore, ready-run release build, activation/rollback, errors | Real browser upload/process/review/deactivate/restore/build/activation exercised. The original legacy release correctly refused rollback because its immutable configuration hash failed validation. The final official-source A→B→A browser check uses an exact validated duplicate, awaits both HTTP200 responses, verifies one active pointer and unchanged saved history/answers/evidence, and restores A. No integrity check was bypassed. See the exact records below. |
| FE-11 | Secondary administrator evaluation page: distinct protocols, immutable configuration and revision, scheduled/outcome counts, controls, results/export and missing-input guidance | `admin.spec.ts`: actual public-question draft/freeze/start/completed run, one persisted item, downloaded actual export, missing-input draft disables Freeze. Existing profile-study modes display separately; creation/import of conversation/profile protocols remains in their dedicated evaluation tools. No gold/support entry exists in learner chat. |
| FE-12 | Light responsive design, keyboard/native dialogs, English labels and handover, nginx deployment files | Browser and zoom evidence below; `docs/frontend.md` gives startup, controls, evaluation separation and limits. Separate clean Docker deployment verified through a real browser. Physical device and human usability review remain pending. |
| CHAT-06 | `/chat` default product; full natural-language follow-ups, sources, copy, feedback, suggestions, stop/retry and persistence | `chat.spec.ts`, `lifecycle.spec.ts`, `session-controls.spec.ts`, `verify-clean-install.mjs`, `verify-openstax-release.mjs`; authored-fixture and genuine OpenStax chat both run without evaluator services/SciQ. Mock runs establish wiring and persistence, not live semantic quality. |

## Responsive refinement coverage

| Subcheck | Recorded checks | Evidence |
|---|---|---|
| UI-01 | Widths 320, 375, 390, 640, 768, 1024, 1280, 1440, 1920, 2560 and breakpoint edges 639/641/1023/1025. Document/body and visible control bounds checked. | `login-responsive-bounds.json`, `chat-responsive-bounds.json`, `stress-layout.json`, administrator tests and screenshots |
| UI-02 | One loaded chat follows 1440 → 900 → 390 → 768 → 1440, then boundaries; same session, draft and selection retained, zero duplicate message submissions. | `chat.spec.ts`, `chat-responsive-bounds.json` |
| UI-03 | Chromium native Appearance > Page zoom at 125%, 150%, 200%, 400%; measured DPR 1.25/1.5/2/4 with visualViewport scale 1; final CSS viewport 314px in a fixed native window. Sources remain operable, draft/revision preserved. | `scripts/verify-browser-zoom.mjs`, `real-browser-zoom.json`, `native-browser-zoom-*.png` |
| UI-04 | 35 paragraphs, long URL, unbroken word, fenced code, 16-column table, formula and final paragraph all retained. Code/table scroll to their far edge locally; normal prose wraps. Long document title reviewed in administrator dialog. | `lifecycle.spec.ts`, `stress-layout.json`, `Markdown.test.tsx`, administrator corpus journey |
| UI-05 | 844×390 landscape and low-height native zoom keep the send/stop control in viewport. Dialog close remains reachable. | `chat-landscape.png`, native zoom metrics; physical mobile soft keyboard and actual orientation hardware **not verified** |
| UI-06 | Source drawer fills narrow screen, original full passage readable, Escape/touch close restores citation focus; sidebar close restores its trigger. Source panel close preserves transcript scroll. | `chat.spec.ts`, `lifecycle.spec.ts`, `touch.spec.ts`, source screenshots |
| UI-07 | Resize a real in-flight job then stop/retry; failed latest regeneration remains visible after refresh with original answer and feedback. Re-login retains active successful replacement. | `stop-retry.json`, `stress-layout.json`, `no-sciq-journey.json` |
| UI-08 | Completing a sixth real response while reading at scrollTop 0 does not jump; Jump to latest returns to bottom; evidence panel does not alter reading position. | `lifecycle.spec.ts`, `stress-layout.json`; 44px Jump button |
| UI-09 | Enter/Shift+Enter, synthetic IME composition guard, focus restoration, no-hover source/feedback touch operation. Essential action buttons have at least 44px targets; inline citations also have a full-size Sources action. | Component tests, `touch-controls.json`, real browser keyboard checks. Physical touch hardware/native IME/screen-reader review **not verified**. |
| UI-10 | Login at all listed widths, profile forms and revision error at narrow widths, corpus document/release forms and experiment detail/export/missing-input states at 320–2560. Actual 100-row history pagination loads 102 and then 104 saved rows without dropping content, changing the reading anchor or losing the draft. | `login.spec.ts`, `account*.spec.ts`, `admin.spec.ts`; `history-pagination/2026-09-08T08-55-45-630Z/verification.json` under the frontend reports |
| UI-11 | Mock badge and original source links remain visible; applied profile snapshot and profile-off request verified; source unavailable and ordinary errors stay explicit. | `chat.spec.ts`, `Chat.test.tsx`, real source screenshots and error traces |
| UI-12 | Screenshots reviewed with geometry and persisted IDs, no shell clipping used to conceal overflow; complete no-SciQ journey rerun and independent clean Compose browser verified. | Timestamped browser results, `no-sciq-journey.json`, `clean-install-browser.json` |

## Final verification snapshot

Component regressions: 23 passed in `evidence/ui/legacy-mcq-final-component-tests.json`, including answered and refused historical MCQ history. Generated API types: drift check passed against the 54-path exported contract at its recorded checkpoint. Production TypeScript/Vite build passed after the MCQ test addition; the math-enabled JavaScript bundle exceeds Vite's 500 kB advisory threshold (approximately 208 kB gzip), with no build failure.

Native browser zoom and independent clean Compose browser smoke passed. The clean deployment journey used `http://127.0.0.1:55173`, two actual turns, four persisted messages, exact source text, refresh retention, narrow sidebar interaction and no page errors.

The full official-source learner browser journey passed at `artifacts/reports/frontend/openstax/2026-09-08T08-27-08-234Z/verification.json`. The valid release rollback passed at `artifacts/reports/frontend/openstax-rollback-roundtrip/2026-09-08T08-42-25-678Z/verification.json`. Those verify release `39483e7f-efbe-42e7-855e-469fd924383e` and their recorded runtime. The later focused parser-v5 continuity check passed at `artifacts/reports/frontend/openstax-v5/2026-09-08T08-54-45-868Z/verification.json`, and actual default-size history pagination passed at `artifacts/reports/frontend/history-pagination/2026-09-08T08-55-45-630Z/verification.json`. Earlier failures are retained with their actual outcomes.

The targeted administrator/account result is `artifacts/reports/frontend/runs/2026-09-08T07-02-19-133Z/results.json`: four tests passed and the corpus rollback test failed with the recorded integrity rejection; its traces are under `2026-09-08T07-02-19-505Z/browser-artifacts/`. Subsequent isolated session-controls (`2026-09-08T07-04-16-150Z/results.json`) and emulated-touch (`2026-09-08T07-05-28-822Z/results.json`) runs each passed. Native zoom evidence was captured with real browser settings. The final official-source widths include the later 44px Jump to latest target adjustment; older zoom and stress images remain evidence of their stated scopes, not a claim that every check was repeated on the final runtime.

Outstanding external acceptance: physical mobile keyboard/orientation behavior, native operating-system IME, screen-reader review, human usability approval, live-provider semantic quality and educational effectiveness. These are distinct from the completed local software checks and are not inferred from screenshots or synthetic events.

## Official-source attribution follow-up

The subsequent FE-06/UI-06/UI-11 update consumes optional `source_url` and `license` fields frozen in the evidence contract. `Chat.tsx` displays the requested OpenStax/Rice attribution and original-source link only for HTTPS `openstax.org` or its subdomains. It does not infer publisher identity from titles or passage text. Missing provenance, generic fixtures, deceptive hosts and invalid/insecure URLs remain unbranded. External links use `noopener noreferrer`. The locator's figure/formula notice remains visible; wrapping and a 44px source-link target are implemented in `styles/app.css`.

The attribution checkpoint passed 21 component tests, the generated-type drift check against 54 paths/64 schemas and the production build: `evidence/ui/openstax-attribution-component-check.json`. The later final browser run below verifies actual real-corpus provenance at desktop and narrow widths. Its source checks do not relabel the earlier native-zoom run as a new source-specific zoom test.

## Historical v4 official corpus browser retake

The final real-corpus run `artifacts/reports/frontend/openstax/2026-09-08T08-27-08-234Z/verification.json` passed its actual browser/API/worker checks against release `39483e7f-efbe-42e7-855e-469fd924383e`. This run uses the genuine four-book OpenStax corpus and local E5/pgvector retrieval. Answering remains explicitly mock. The earlier license-abbreviation assertion failure at `2026-09-08T08-07-00-041Z`, earlier successful narrower scope at `2026-09-08T08-09-56-620Z`, and verifier navigation-count failure at `2026-09-08T08-24-40-998Z` remain preserved. The final verifier compares the exact saved license and waits for the new-chat route and empty transcript before counting its turns.

The run verifies source text and SHA-256, original official link, saved license details, physical-page locator and the full figure/formula caveat at 1440px and 390px. All returned chunks and processing IDs belong to the published real manifest. It covers five ordinary chat turns, prepared light/simpler/example follow-ups, a glycolysis topic change, profile-off, latest regeneration, immutable original answer/evidence/feedback, re-login with ten retained messages, and explicit Formula One mock refusal. Eleven widths (320, 390, 639, 640, 641, 768, 1023, 1024, 1025, 1440, 1920) preserve the draft with zero duplicate submissions or body/document overflow. Mobile source/navigation controls remain usable. Desktop, 320px conversation and 390px source images were visually reviewed.

The final run additionally verifies a new-session ambiguous question, clarification completion, requested sources and social response. It checks that answered mock excerpts do not finish as cited source exercises. This supplies updated local evidence for FE-03–FE-09/CHAT-06 and UI-01/02/06/07/10/11/12. UI-03 native zoom, UI-04 long stress content, UI-05 physical keyboard/orientation, UI-08 arrival while reading and UI-09 native IME/assistive technology keep their separately stated evidence and limits above. The mock exercise-selection and correction-scaffolding fixes are documented in `docs/execution/mock-evidence-replay.md`; the final browser run uses those fixes. Native extraction spacing and partial chunk boundaries remain visible rather than being silently rewritten.

## Valid release rollback and diagnostic follow-up

`artifacts/reports/frontend/openstax-rollback/2026-09-08T08-36-19-078Z/verification.json` built duplicate B `29f2c946-a664-44f3-bd20-5cb5c886e76a` from A's exact four processing runs and pinned E5 configuration. The embedding, content and configuration hashes matched. A five-second verifier assertion expired before the actual activation response; its cleanup read raced that transaction, and B became active afterward. This is preserved as a failed verifier run, not a rollback pass. The earlier `08-28-32-805Z` attempt was interrupted by an external Docker restart; `08-34-53-457Z` failed a test selector before a build or activation.

The actual administrator browser restored A with a completed HTTP200 response in `artifacts/reports/frontend/openstax-rollback-recovery/2026-09-08T08-39-23-027Z/verification.json`. The final bounded run at `artifacts/reports/frontend/openstax-rollback-roundtrip/2026-09-08T08-42-25-678Z/verification.json` then performed A→existing B→A through the UI, awaiting both completed HTTP200 responses and all in-flight mutations. It verified exactly one active pointer at each stage, unchanged full saved messages/answers/evidence, and the identical source/configuration/vector identities. A remained the sole active release. The 390px and 1440px administrator pages had no document/body horizontal overflow; both screenshots were visually reviewed. No extra source mutation or new build occurred during this final roundtrip.

Generated browser diagnostics were sanitized after an authorization header appeared in a timeout report. `evidence/ui/browser-diagnostic-sanitization.json` records 504 redactions and zero remaining matches at that checkpoint without storing credential values. The attempted logout endpoint returned 404; actual stale-token revocation was subsequently verified with 401 in `evidence/ui/demo-admin-token-revocation.json`. The original outcomes and error categories remain preserved. New verification helpers sanitize errors before writing them. The final read-only `evidence/ui/browser-diagnostic-final-scan.json` scanned 110 files and 488 decompressed archive members, found zero credential matches, and did not overwrite the original sanitization counts.

## Parser-v5 continuity and actual pagination follow-up

After publication of v5 release `4f11bd70-a486-4d16-b216-78cfe499530a`, `artifacts/reports/frontend/openstax-v5/2026-09-08T08-54-45-868Z/verification.json` passed a new two-turn photosynthesis/light conversation through the actual browser/API/worker. All current evidence chunks and processing IDs belong to the v5 manifest. The original exact source text, hash, title, official HTTPS link, license, physical-page locator and figure/formula notice were checked at 390px and 1440px. The earlier A-release answer and citations remain accessible at both widths with unchanged saved response/evidence and entire session history. The current 390px source view and historical 1440px source view were visually reviewed. The first focused verifier omitted the word “the” from its expected notice and failed at `2026-09-08T08-54-05-327Z`; the application notice was correct and unchanged.

`artifacts/reports/frontend/history-pagination/2026-09-08T08-55-45-630Z/verification.json` closes the previously unverified pagination clause. It creates a separate, explicitly labelled UI fixture using 51 actual social API/worker turns, with no corpus upload, direct database insertion or existing-history overwrite. The shipped frontend requests 100 rows, follows the real `after_sequence=100` cursor for the remaining two rows, and displays all 102 IDs in order with their complete content. One further actual UI turn produces 104 rows. Earlier durable fields are unchanged; only computed latest-answer regeneration eligibility is excluded from that comparison. The older-message reading anchor stays within two pixels during the paginated refresh, an unsent draft survives, all row IDs remain through 390px/1440px resizing, and Jump to latest reaches the bottom. The 390px screenshot was visually reviewed.

Pagination is automatic forward-cursor loading before display. There is no “Load older” button, and this report does not claim that interaction. The first helper incorrectly expected job state `completed` instead of the canonical `succeeded`; its failed record at `history-pagination/2026-09-08T08-53-50-322Z/verification.json` is preserved. The correction changed only the verifier. Both final checks use real infrastructure with mock answering and establish software behavior, not scientific or live-model quality. UI-05/UI-09 physical/native/assistive-technology gaps and the full FE-12/AC-44 human/device acceptance boundary remain unchanged.

The updated canonical-status verification is `evidence/ui/status-reconciliation-pagination.json`: all twelve current subcheck statuses use the five project labels, and every cited evidence path exists. The later read-only `evidence/ui/browser-diagnostic-post-pagination-scan.json` checked 116 generated diagnostic files and 488 decompressed trace members with zero credential matches, preserving the earlier sanitization and scan records.
