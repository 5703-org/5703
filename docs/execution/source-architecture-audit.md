# Source architecture and current evidence reconciliation

Snapshot: 8 September 2026. The original first audit is retained by its saved checkpoint; this current mapping is updated from actual source and runtime evidence, without claiming whole-project scientific acceptance. The latest user instruction requires documentation reconciliation first, then the full official OpenStax corpus and real local embeddings. Only answer generation may remain mock. Earlier audit wording that deferred learned embeddings or ordinary official-data acquisition is superseded.

## What was actually supplied

The two supplied v5 Word documents were re-extracted in paragraph/table order and checked alongside the ZIP English master, task/check registries, and the complete responsive refinement. Exact paths, hashes, paragraph counts, embedded-media inventory and task/check coverage are recorded in `evidence/source_audit/source_read_manifest.json`. The Word documents define required work; their references to historical tests are not new execution evidence. Their scope, three modes, lean rules, all 108 tasks, all 60 checks and reporting-only owner/week allocations remain effective. The responsive document adds UI-01–UI-12 subchecks without changing those totals.

The v5 Word documents contain no embedded architecture images. Actual original architecture materials are in the extracted COMP5703 source package:

| Source, relative to `development_inputs/sources/COMP5703` | Exact location and visual inspection | Meaning |
| --- | --- | --- |
| `CS30-1_Project_Framework_and_Delivery_Workflow.pdf` | Physical/printed page 1, Figure 1, caption “Proposed system framework; components remain configurable for controlled comparison.” Render inspected: `evidence/source_audit/original-framework-page-1.png` | Original full-system architecture, prepared 19 August 2026; primary mapping below |
| `tut5/xianshu/architecture-en-preview.png` | Entire supplied image; no pagination | Original three-layer E0/E1 baseline diagram; SciQ MCQ experiment is historical baseline, not the current learner entry point |
| `了解项目/CS30-1_Project_Overall_Requirements_CN.pdf` | Physical/printed pages 5–6, section 3; both rendered and inspected | Earlier six-module grid and numbered offline/online processing steps; page 5 starts the grid and page 6 continues it |
| `tut5/Chong Zhang/Framework.pdf` | Physical page 1, section 1 “Evaluation Architecture”; rendered and inspected | Original evaluator graph and fixed-test-set design |
| `tut5/xianshu/basicframework.png` | Entire supplied screenshot inspected | Repository screenshot with a partially visible README diagram; it is not a complete replacement for the architecture image |

No new diagram is presented as an original. The ledger's source manifest records the original hashes and search inventory. A diagram's intended connection does not itself establish that the software connection runs.

## Original full-system Figure 1: every module

Statuses are scoped to the named component. **REAL_FLOW_VERIFIED** means the cited real parser, database, API or browser flow ran; it does not imply real answer generation or a complete official corpus. **MOCK_TEST_PASSED** means the cited mock/double behavior passed. **IMPLEMENTED_UNVERIFIED** means an implementation exists but the required real execution is missing or a material acceptance gap remains. **NOT_IMPLEMENTED** means the required artifact/result is absent. **WAITING_EXTERNAL** is reserved for a genuine unavailable human/provider/source input, not ordinary work that can proceed locally.

| Original module | Implementation and evidence | Current status and precise limit |
| --- | --- | --- |
| Trusted Educational Documents — PDF/slides/notes | Official acquisition and hash/license inspection of all four full PDFs; `evidence/openstax/acquisition-run.json`, `pdf-source-inspection.json` | **REAL_FLOW_VERIFIED** for the required four-book PDF corpus. Slides/notes are historical examples; v5 executable ingestion is PDF/TXT. |
| Parse, Clean and Chunk — metadata/versioning | `pypdf_bookmarks_v5`, source-specific reviews, 14 local OCR transcripts and one publisher description; `evidence/openstax/v5/formal-source-validation.json` | **REAL_FLOW_VERIFIED**:4,638 pages accounted,5,541 units,73 justified exclusions,15 supplemented originals,10,594 span/hash-verified chunks. Full visual/math semantics and independent scientific review remain unverified. Exact historical pilot cover/blank pages were reviewed. |
| Embedding Models — compare configurations | `retrieval/embedding.py`, actual pinned E5 checkpoint/tokenizer/CUDA execution; `evidence/corpus/e5_download.json`, `evidence/openstax/v5/publication.json` | **REAL_FLOW_VERIFIED** for10,594 actual384-dimensional E5 vectors; independent model comparisons remain separate work. |
| Vector Knowledge Base — source-traceable chunks | PostgreSQL16/pgvector0.8.6, complete integrity checks and atomic release; `evidence/openstax/v5/publication.json` | **REAL_FLOW_VERIFIED**: active release4f11bd70-a486-4d16-b216-78cfe499530a,10,594 actual vectors, exact lineage/hashes; old answers/citations/originals unchanged. |
| Question + Student Profile — level/style/context | `frontend/src/Chat.tsx`, profile settings, learning/answering models, `personalisation/compiler.py`, `conversation/context.py` | **REAL_FLOW_VERIFIED** for HTTP persistence, profile revisions and included history; prompt/semantic response observations use **MOCK_TEST_PASSED** providers. |
| Query Processing — rewrite/filters | `conversation/query.py`, context/summary selection, shared answer service | **MOCK_TEST_PASSED** for authored dependent-reference/correction/topic fixtures and real request traces. Broad real-model meaning preservation is unverified. |
| Retrieve + Rerank — semantic/hybrid/Top-K | Shared R0/R1/R2 and configurable R3; `evidence/openstax/v5/retrieval-verification.json`, `evidence/corpus/retrieval-comparison.json` | **REAL_FLOW_VERIFIED** for15 real E5/pgvector queries and unavailable-source filtering. Query SQL reduced7,721→6 with identical hits; single-run diagnostic timings are not an SLA. Actual R1/R2 and pinned R3 inference are separately recorded in `docs/retrieval_diagnostics.md`; formal relevance remains unverified. |
| Prompt + LLM — evidence-constrained | `generation/prompt_builder.py`, versioned prompts, adapters/parser/service; one shared application execution path | **MOCK_TEST_PASSED** for strict prompts, mode isolation, format repair and simulated transport failures. Live answer calls are deliberately deferred by the user. |
| Grounded Answer + Citations — safe refusal when weak | `contracts/models.py`, answering service, evidence snapshots/router, frontend response/source viewer | **MOCK_TEST_PASSED** for response policy. **REAL_FLOW_VERIFIED** for storing and opening exact original authorised passage/hash, request-local citation identity and source revocation. Human factual support is unverified. |
| Evaluation & Observability — fixed test set, retrieval metrics, faithfulness, level fit, latency/cost, versioned experiments | `evaluation/`, experiment bridge/models/router, real durable jobs and independent study records; `evaluation/software_verification.log` | **MOCK_TEST_PASSED** for 54 evaluator checks, including 10 actual PostgreSQL checks and 40 authored conversational turns. Actual full SciQ acquisition and controlled retrieval diagnostics are recorded separately; independent qrels/ratings are **WAITING_EXTERNAL**. Usage/cost absence stays null. |

## Original full-system Figure 1: every drawn connection

| Drawn connection | Actual implementation connection | Evidence and remaining boundary |
| --- | --- | --- |
| Documents → Parse/Clean/Chunk | Official immutable originals → registered versions → durable full processing jobs → retained raw/clean units and chunks | **REAL_FLOW_VERIFIED**, all four official full PDFs; exact source/quality/span evidence in formal-source-validation.json |
| Parse/Clean/Chunk → Embedding Models | All10,594 ready chunks → pinned E5 tokenizer/prefix/window checks and actual CUDA encoding | **REAL_FLOW_VERIFIED**, full corpus passes model-window checks (maximum353tokens including prefix/special tokens) |
| Embedding Models → Vector Knowledge Base | Actual384-dimensional E5 vectors → release_chunks → integrity validation → active pointer | **REAL_FLOW_VERIFIED**,10,594 vectors and immutable complete release; no simulated vector substitution |
| Question/Profile → Query Processing | Owned saved message + frozen context/profile → prepared query | Actual API/DB context and mock prompt traces; source-independent user profile persistence verified |
| Query Processing → Retrieve/Rerank | Prepared standalone query → shared RetrieverPort with release/top_k/visibility | Actual shared service, current prepared-query traces and official E5 query vectors; benchmark stems bypass rewriting |
| Vector Knowledge Base → Retrieve + Rerank | Actual E5 query vectors → PostgreSQL cosine ordering → source-attributed candidates | **REAL_FLOW_VERIFIED** forR0 and15scheduled questions; actual R3 inference has separate versioned evidence; formal relevance comparisons remain unverified |
| Retrieve/Rerank → Prompt/LLM | Whole selected evidence chunks + budget decisions → current request prompt | Spy adapter verifies actual selected text and IDs; no historical assistant answer becomes a source |
| Prompt/LLM → Grounded Answer/Citations | Provider attempt → strict selected-mode parse → atomic publication → evidence viewer | Mock/simulated output and real DB/browser publication verified; live semantic quality not inferred |

The Evaluation & Observability footer has **no drawn arrows** to the other boxes. Its implementation relationships to persisted requests, attempts, evidence, evaluator-only references and exports are additional mappings from the v5 specification, not invented original connectors. The grid on overall-requirements pages 5–6 similarly shows numbered modules rather than connector arrows; its explicit numbered steps support the same offline and online chains.

## Original baseline image and evaluator graph

The baseline PNG contains OpenStax Textbooks → Document Parsing (OpenStax API/PDF) → Structure-aware Chunking → Dense Embedding (BGE/E5/GTE) → Vector Store (FAISS/pgvector). Those modules map to the offline rows above. The current choice is pgvector, with PDF/TXT ingestion; alternatives in parentheses are not a requirement to build both stores or every embedding family. Real E5 execution and four-book dense retrieval are now verified by the current publication evidence.

The image's online layer branches SciQ Questions into E1 Basic Dense RAG → Dense Retrieval Top-k → Retrieved Context → Prompt Builder → Evidence-constrained Prompt → LLM Adapter → Online/Open-source LLM → Structured Answer; E0 Pure LLM Baseline joins at the adapter and bypasses retrieval. Each sequential connection maps to `evaluation/datasets/`, `backend/app/modules/experiment/bridge.py`, shared answering/retrieval/generation and strict schemas. The real PostgreSQL evaluator tests prove both E0 schemas call no retriever and both E1 schemas use the shared engine with actual configured top_k. Their answer and embedding providers are mock. The original diagram does not visibly draw a connector from the vector-store box into online dense retrieval; that necessary implementation dependency is explicitly an inference, not a recovered arrow.

Its evaluation layer compares E0/E1 → SciQ MCQ Accuracy → the goal of validating whether RAG outperforms a pure LLM. `evaluation/metrics/` and paired analysis implement the measurement path, but actual official-data scores and any claimed improvement are **NOT_IMPLEMENTED** results. The current product adds natural chat, real history/profile/feedback and separate SciQ-derived OpenQA; these v5 additions must not be retroactively attributed to the old MCQ-only input picture.

The original QA PDF graph is Fixed SciQ Evaluation Set → Gold Answer/Gold Evidence → Evaluation Record → System Prediction → Retrieved Evidence → Evaluation Engine → Answer Quality/Retrieval/Runtime → Evaluation Result. It describes evaluator record/analysis relationships, not permission to pass gold to the answer engine. Current mapping: dataset private/public adapters → evaluator-only references/qrels and frozen records → shared-engine receipts → outcomes with retrieved snapshots → mode-aware metrics/timings → separate exports. All branches have software fixtures. Actual SciQ is acquired; real retrieval/runtime observations are separate from unavailable judged evidence, live answer-effectiveness results and human ratings. Gold/support are absent from application DTOs, tables, provider prompts and application runtime mounts.

## Four-book scope and the actual missing work

`tut5/HongleYang/WEEK4_SUMMARY_AND_WEEK5_PLAN.md`, lines 12–14, explicitly freezes **Biology 2e, Chemistry 2e, Anatomy & Physiology 2e, and Concepts of Biology**, excludes retired **College Physics**, and labels SciQ/OpenStaxQA as evaluation inputs. Its section 1.4 explicitly calls production parsing **Pilot pending**. Claims about 13 historical raw entries do not establish that their bytes are in the supplied archive. Referenced historical manifests must be recovered and verified or recorded as missing; never reconstruct them as original evidence.

The current active release is the complete four-book corpus, with 10,594 actual E5 vectors. [Per-book execution report](openstax-corpus-report.md) records sources, current versions, full scope, source/raw/storage hashes, page recovery, actual counts and query excerpts. [Publication proof](../../evidence/openstax/v5/publication.json) records the atomic switch and unchanged historical original/answer/evidence records. The 73 excluded units are reviewed covers, blanks or running furniture; 15 substantive source pages were recovered rather than silently discarded. Full visual/equation fidelity and independent human scientific review remain unverified.

The newly downloaded Concepts of Biology SHA256 exactly matches historical pilot `da0ffc8585e172f04eb0abb08949087baf85cc229ef2736ef06e5aa17094b1f6`. Its physical pages1–2 were inspected as cover/blank; original pilot records remain untouched. All source relationships are backed by actual full-corpus validation, not by historical download claims.

## Reconciled ledgers and limits

`tasks.json` retains all original 108 task IDs, requirements, dependencies, owners and acceptance references; `acceptance.json` retains AC-01–AC-48 and HC-01–HC-12. Every row now has one of the five user-requested statuses, concrete component/evidence mappings and remaining scope. No completion date is invented. The historical first pass remains at `evidence/source_audit/ledger_reconciliation.json`. The [final current reconciliation](../../evidence/source_audit/final-ledger-reconciliation.json) checks exact original requirement text/IDs/dependencies, all 12 initial responsive mappings, current cited paths and passing test nodes, immutable source hashes, published v5 corpus and the final unchanged-source gate. It validates current evidence consistency; it does not itself rerun application tests.

Existing tests remain attributed to their actual saved commands/logs. The AI 81/26/14 runs overlap and must not be summed. The earlier evaluator-only checkpoint had 54 tests, including 44 unit/application and 10 PostgreSQL checks. The final separate full gate passes 231 Python and 23 frontend checks; overlapping runs are not added together. The current browser/API/DB source flows include official OpenStax and learned E5; only answering stays mock. A real valid A→B→A browser roundtrip passed with identical vector/content hashes and history, and a focused v5 browser check opened current and historical citations after the new publication. Prior integrity rejection, interrupted attempts and verifier waiting failures remain preserved. Physical device, native IME and human review cannot be inferred from emulation. Official corpus acquisition, actual learned embeddings and publication are now executed. Final source packaging and its independent installation are recorded separately from the passed software gate. They require no new external approval.


## Current parser revision and immutable lineage

The v5 rebuild and actual-source diff are under `evidence/openstax/v5/`. All 5,541 raw/cleaned source-unit texts and original issues are unchanged. Twenty synthetic page-label fields on pages2–6 were corrected; 18 prior frontmatter chunks became28, while10,566 body chunks retain exact text/spans and bit-identical real vectors. The prior v4 corpus remains retired and historical citations remain accessible; it has not been rewritten as if it had always used v5. Current full verification checked50 deterministic chunk samples against63 original PDF pages, all source/vector relationships and15 actual queries. The formal active version is4f11bd70-a486-4d16-b216-78cfe499530a.
