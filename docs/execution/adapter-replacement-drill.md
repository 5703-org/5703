# Configurable adapter replacement: 8 September 2026

The shared generation consumer now has an actual configuration-switch drill. Five focused tests passed: three successful mode cases and two failure cases. [verification.json](../../evidence/operations/adapter-replacement/verification.json), [JUnit](../../evidence/operations/adapter-replacement/pytest.xml) and [output](../../evidence/operations/adapter-replacement/pytest.log) retain the execution details. The new test is [test_adapter_replacement.py](../../tests/unit/test_adapter_replacement.py).

One unchanged `GenerationService` instance selects the built-in mock transport, then the configured local HTTP transport, then the mock again. Six real loopback requests reached an owned ephemeral HTTP fixture; there was no injected adapter, patched `urlopen`, external provider or paid call. The fixture supplied distinct, valid authored responses and explicit usage values.

The checks cover interactive chat, stem-only OpenQA and MCQ through their normal prompt builders, transport, parser and result object. Chat retains its exact history and compiled policy; benchmarks remove both. Transmitted source objects, citation IDs, role messages, selected schema, model/output settings, provider request identity and usage propagate correctly. MCQ option text matches its selected candidate. Returning to mock does not retain the HTTP configuration or make another HTTP request. A real loopback HTTP 401 produces the standard nonretryable error; invalid citations consume one format repair and then remain an error, without a mock fallback.

Six relevant runtime and API/UI consumer files match the already tested candidate 2 bytes. This addition changes only a focused test and its evidence. No database, main service, corpus or user history was changed. The first test attempt had one assertion failure because it compared a multiline policy with its JSON-escaped representation. That JUnit is retained. The corrected assertion decodes the actual context object and checks the exact policy and evidence; no runtime or response fixture changed.

For AC-34, this closes the previously missing **model-adapter software switch** evidence, alongside the existing actual PDF/TXT and PostgreSQL R0/R1 dispatch evidence. Its appropriate component label is `MOCK_TEST_PASSED`. The runtime's `model_mode=live` value identifies its configured non-mock transport branch; the endpoint here is an authored test fixture, not a learned model. No live-provider connectivity, factual effectiveness or output quality is established. This focused drill does not itself run a database-backed answer job; separate existing API, persistence and browser evidence retain their own scope.
